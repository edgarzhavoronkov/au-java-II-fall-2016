package ru.spbau.mit.torrent.io;

import ru.spbau.mit.torrent.utils.TorrentFile;

import java.util.Collections;
import java.util.Map;

/**
 * Created by Эдгар on 05.12.2016.
 */
public class ClientSerializer {
    public static Map<Long, TorrentFile> loadFiles(String workingDir) {
        return Collections.emptyMap();
    }

    public static void saveFiles(String workingDir, Map<Long, TorrentFile> files) {

    }
}
